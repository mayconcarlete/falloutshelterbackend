version: 2.1


jobs:
  unit-test:
    machine: true
    image: docker/base-tests.Dockerfile
    steps:
      - checkout
      - run:
          name: Unit Tests
          command: make unit-test  

workflow:
  tests:
    jobs:
      - unit-test
